# 恢复与重生成策略：Intuitive Surgical 专利汇总报告 (v2.0)

## 1. 现状评估 (Status Audit)
- **原始报告**: `intuitive_vision_full_report_all.md`
- **核心错误**:
    - 统计偏差：错误地将 81 个公开文本计为 81 项发明（实际应为 59 族）。
    - 核心缺失：`US20220015832A1` 被误删且未在分析中体现。
    - 语义漂移：将机械系统（如 US9283050B2）幻觉为 AI 视觉追踪。

## 2. 修正方案：全量重生成 (Total Regeneration)
舍弃对原始 Markdown 的局部修改，采用基于 IR（中间表示层）的“协议驱动”重生成流程。

### 步骤 1：数据层清理 (Data Layer)
- **工具**: `patent-data-pipeline/deduplicate.py`
- **目标**: 将 `downloaded_patents/` 目录下的 81 个文件提纯为 59 个唯一家族 ID。
- **基准校验**: 运行 `baseline_verify.py`，若 `US20220015832A1` 仍缺失，则必须重新检索并下载。

### 步骤 2：审计层分析 (Audit Layer)
- **工具**: `patent-claims-analyzer` (High-Intensity Audit Mode)
- **逻辑**: 执行“冲突对撞协议”。
- **产物**: 填充 `structured_findings.json`。每一项特征必须具备：
    - `{标签, 权利要求引文, CPC 锚点}`

### 步骤 3：生成层输出 (Generation Layer)
- **工具**: `patent-application-creator` (Read-Only Mode)
- **规则**: 严格仅从 JSON 事实基座中读取数据。
- **输出**: 包含证据链的全新汇总报告。

## 3. 验收标准 (Acceptance Criteria)
- [ ] 专利总数显示为 59。
- [ ] 报告中包含对 `US20220015832A1` 的详细分析。
- [ ] `US9283050B2` 的描述修正为“机械控制”，无 AI 幻觉词汇。
- [ ] 所有技术断言均附带具体的引文（Column/Line 或 Claim No.）。
